<p id="notice"><%= notice %></p>

<h1>Leaderboards</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Cscore</th>
      <th>Fscore</th>
      <th>Bscore</th>
      <th>Hscore</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @leaderboards.each do |leaderboard| %>
      <tr>
        <td><%= leaderboard.user %></td>
        <td><%= leaderboard.Cscore %></td>
        <td><%= leaderboard.Fscore %></td>
        <td><%= leaderboard.Bscore %></td>
        <td><%= leaderboard.Hscore %></td>
        <td><%= link_to 'Show', leaderboard %></td>
        <td><%= link_to 'Edit', edit_leaderboard_path(leaderboard) %></td>
        <td><%= link_to 'Destroy', leaderboard, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Leaderboard', new_leaderboard_path %>
